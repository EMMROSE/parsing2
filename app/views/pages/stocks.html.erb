<div>
  <%= link_to 'retour au Menu', root_path, class: "btn btn-success"%>
  <ul>
    <% Product.all.each do |product| %>
      <li><%= product.id %> - <%= product.name %> - <%= product.status %> <%= link_to 'Marquer comme vendu', sold_status_path(product), method: :post, class: "btn btn-success"%> <%= link_to 'Marquer comme retourné', return_status_path(product), method: :post, class: "btn btn-warning"%> <%= link_to 'Supprimer', product_path(product), method: :delete, class: "btn btn-danger"%></li>
      <br>
    <% end %>
  </ul>
</div>

